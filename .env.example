# SenseForge Environment Configuration - PRODUCTION READY
# Copy to .env and configure with your actual values
# NEVER commit .env to version control

# ===== OPERATING MODE =====
SENSEFORGE_MODE=mock  # Options: mock (demo), live (production)

# ===== SECURITY CONFIGURATION =====
# Enable authentication (REQUIRED for production)
ENABLE_AUTH=false  # Set to 'true' in production
ENABLE_HTTPS_REDIRECT=false  # Set to 'true' in production

# Secrets management mode
SECRETS_MODE=env  # Options: env, file, vault

# Master password for encrypted secrets file (if using file mode)
# MASTER_PASSWORD=your-secure-master-password-here

# Encryption key for data at rest (REQUIRED for production)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ENCRYPTION_KEY=your-32-byte-base64-encoded-key-here

# Audit trail signing key (REQUIRED for production)
# AUDIT_KEY=your-signing-key-here

# ===== API KEYS =====
# Leave empty for mock mode
# For production, obtain keys from respective services

# Cambrian API (https://cambrian.org)
CAMBRIAN_API_KEY=

# Letta Memory API (https://letta.com)
LETTA_API_KEY=

# Ambient LLM API (https://ambient.ai)
AMBIENT_API_KEY=

# ===== DATABASE CONFIGURATION =====
# SQLite (development only)
DATABASE_URL=sqlite:///senseforge.db

# PostgreSQL (production - recommended)
# DATABASE_URL=postgresql://user:password@localhost:5432/senseforge

# ===== REDIS CONFIGURATION =====
# For distributed rate limiting and caching
# REDIS_URL=redis://localhost:6379/0

# ===== RATE LIMITING =====
RATE_LIMIT=100  # Requests per minute

# ===== ALLOWED HOSTS =====
# Comma-separated list of allowed hostnames
# Use * for development only
ALLOWED_HOSTS=*

# Production example:
# ALLOWED_HOSTS=senseforge.example.com,api.senseforge.example.com

# ===== SSL/TLS CERTIFICATES =====
# Required for production HTTPS
# SSL_KEYFILE=/path/to/private.key
# SSL_CERTFILE=/path/to/certificate.crt

# ===== MODEL CONFIGURATION =====
JEPA_CHECKPOINT=checkpoints/jepa_model.pth

# ===== LOGGING =====
SENSEFORGE_LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR

# ===== DASHBOARD =====
SENSEFORGE_REFRESH_INTERVAL=5  # seconds

# ===== EXTERNAL SERVICE TOGGLES =====
# Disable services that aren't available
ENABLE_CAMBRIAN=true
ENABLE_LETTA=true
ENABLE_AMBIENT=true

# ===== MONITORING =====
# PROMETHEUS_URL=http://prometheus:9090
# GRAFANA_PASSWORD=your-grafana-password

# ===== SECURITY CHECKLIST =====
# Before deploying to production:
# [ ] Set SENSEFORGE_MODE=live
# [ ] Set ENABLE_AUTH=true
# [ ] Set ENABLE_HTTPS_REDIRECT=true
# [ ] Generate and set ENCRYPTION_KEY
# [ ] Generate and set AUDIT_KEY
# [ ] Configure DATABASE_URL with PostgreSQL
# [ ] Configure REDIS_URL
# [ ] Set ALLOWED_HOSTS to specific domains
# [ ] Configure SSL_KEYFILE and SSL_CERTFILE
# [ ] Add all API keys
# [ ] Review and set RATE_LIMIT appropriately
# [ ] Change default passwords
# [ ] Scan git history for leaked secrets: git log -p | grep -i "api_key"
# [ ] Enable monitoring/alerting
# [ ] Test health checks
# [ ] Run security scan: pip install safety && safety check
